import type { EquipmentOrigin } from '../../../data/dtos/equipment';

/** Spanish labels for the equipment origin (11). */
export const EQUIPMENT_ORIGIN_LABELS: Record<EquipmentOrigin, string> = {
  externo: 'Externo',
  venta: 'Venta',
  renta: 'Renta',
};
