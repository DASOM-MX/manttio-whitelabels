/** `surface-1000` of the **fixed** chrome neutral (plan 22 § Target 3 — hue 0,
 *  saturation 0%, lightness 10%), as hex for the editor's contrast math.
 *  Surface left the brand contract, so this value is a constant, not something
 *  read back off a tenant scale. */
export const FIXED_SURFACE_DARK_HEX = '#1A1A1A';
