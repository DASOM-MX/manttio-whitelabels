/** Monday-first single letters for the year view's mini-months, where there is
 *  no room for `Lun`/`Mar`. The two `M`s are the conventional Spanish
 *  abbreviation and are not a mistake — position disambiguates them, which is
 *  why every printed calendar does the same. */
export const WEEKDAY_INITIALS: string[] = ['L', 'M', 'M', 'J', 'V', 'S', 'D'];
