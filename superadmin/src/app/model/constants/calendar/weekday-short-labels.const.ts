/** Monday-first, matching the week grid's columns. Hand-rolled because the app
 *  registers no Angular locale — `date: 'EEE'` would render English. */
export const WEEKDAY_SHORT_LABELS: string[] = ['Lun', 'Mar', 'Mié', 'Jue', 'Vie', 'Sáb', 'Dom'];
