/* eslint-disable prettier/prettier */
export interface Customer {
    id: string;
    name: string;
    identification: string; // personal o empresarial
    phone: string;
    email: string;
    observation: string;
}
